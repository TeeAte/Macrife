# Macrife v1.6 发布说明

Macrife 是专为 Apple Silicon 架构打造的 macOS 原生 AI 视频插帧工具，基于 RIFE 算法、CoreML 与 Metal 原生着色器深度优化构建，提供纯离线、极速、无损细节的视频超高刷补帧体验。

v1.6 带来了**全架构硬件自适配引擎**、**新增 720p 黄金处理精度档位**、**超高刷（120fps）显存熔断与 Swap 免疫机制**以及全方位的调度与交互升级！

---

## 📦 下载与校验

| 文件 | 说明 |
|---|---|
| `Macrife-1.6.dmg` | 完整安装包（双击挂载后将 Macrife 拖拽进 Applications 即可使用） |
| `SHA256SUMS.txt` | 完整性校验文件 |

```bash
# SHA256 校验
shasum -a 256 Macrife-1.6.dmg
# 对应校验值: 59b0a7d6fc60c10e2182e44b37916b0c6d5dc84e032fb2931ef7d30012513d2d
```

---

## 🚀 v1.6 核心更新亮点

### 1. 🖥️ 全架构硬件自适配引擎（HardwareProfile）
- **自动探查宿主机硬件**：通过 IOKit、Metal 与 ProcessInfo 自动感知 Apple Silicon 芯片（M1 ~ M4 基础款 / Pro / Max / Ultra）、GPU 核心数（如 14/16/30/40 核）、统一内存规格（8GB / 16GB / 32GB+）与 CPU 核心；
- **在途缓冲深度动态自适应（`autoMaxInFlight`）**：
  - **紧凑级 (≤8GB 内存)**：高倍率 4K 下在途窗口强制锁定为 2，默认采用 1:1 双擎，节省 800MB 常驻模型显存，彻底杜绝 Swap 磁盘换页卡顿；
  - **标准级 (12~24GB 内存)**：动态在途控制，4K 120fps 锁死在安全线（<560MB 缓冲），常规 60fps 保持 5~6 顺畅推进；
  - **充裕级 (≥32GB 内存)**：放宽至 8~10，彻底消除快慢 worker 之间的反压等待气泡；
- **动态 UI 呈现**：消除界面硬编码文案，顶部直接展示当前设备硬件画像徽章（`当前设备：Apple M1 Pro（14核 GPU · 16GB 统一内存 · 8核 CPU）`），并在内存紧张搭配时提供智能预警。

### 2. 🌟 新增 720p（1280×704）处理精度黄金甜点档位
- **跨度平滑**：填补 576p（58.9万像素）与 1080p（208.8万像素）之间高达 3.54 倍的巨大算力断层；
- **兼顾质感与极速**：
  - 仅需 576p 1.53 倍的算力（1080p 的 43%），光流采样网格密度提升 53%；
  - 双擎并发下可跑出 **20 ~ 25 fps** 稳定帧率（是 1080p 的近 3 倍）；
  - 在体育赛事、无人机快速平移、复杂发丝与雨丝飞舞等高频剧烈运动场景下，光流稳健性与边缘抗破损能力显著提升。

### 3. ⚡ 超高刷 120fps 性能暴跌根治与内存优化
- **Step 级 Autoreleasepool 隔离**：在插值任务循环内部引入帧级内存解包，单帧执行完毕立即释放 CoreML 临时张量，彻底解决 120fps 单步 4 帧连续推理导致的临时对象堆叠；
- **高倍率动态窗口收紧**：自动检测 `multiplier >= 3.5`（如 24 $\to$ 120fps），动态收缩在途任务深度，杜绝 4K 像素缓冲膨胀引发系统虚拟内存换页（Swap 磁盘 I/O）。

### 4. 🚀 多核异构并发与双擎调度策略全面升级
- **三种算力核心模式**：
  - `🍃 节能模式`：纯 ANE 神经引擎推导，整机功耗仅 ~5W，发热极低，日常推荐；
  - `⚡ 独显模式`：全速驱动 GPU 满频矩阵算力；
  - `🚀 双擎并发`：ANE + GPU 协同作战，全速输出；
- **四种双擎调度策略**：
  - `🚀 ANE + GPU (1:1)`：双擎对称并发，轻量均衡，Max/Ultra 及 8GB 首选；
  - `🚀🚀 双 ANE + GPU 三路 (2:1)`：两路 ANE 吃掉 70% 卷积，GPU 不排队，Pro/Base 机型速度最快（推荐）；
  - `🍃🍃 双 ANE 并发 (低功耗)`：双通道 ANE 并发，零 GPU 卷积负担；
  - `🍃🍃🍃 三通道 ANE 极限并发`：纯 ANE 极限推进。

### 5. 🎨 原分辨率融合（Blend at Source）
- 光流在所选模型尺寸计算，像素级的 Warp 与 Blend 在原片尺寸（如 4K）完成；
- 实测 4K 原片保留 **95.7%+** 原生细节，输出视频尺寸永远等于源尺寸，彻底告别低分模型放大发糊。

---

## 🛠️ 历史核心特性继承
- **场景切换检测**：MAD 残差实时检测，硬切镜头处自动定格并跳过插值，消除双重曝光鬼影；
- **无损音频直通**：AAC 原生直通零重编码，PCM/Opus/AC-3 自动重新封装 AAC，音画天然对齐；
- **任务管理**：支持随时暂停 / 继续（暂停零资源开销），支持即时取消；
- **防休眠守护**：执行期间调用系统活动断言防止空闲睡眠（Keep-Awake）；
- **动态预估**：5 秒滑动稳态帧率采样，动态呈现精准剩余时间（ETA）；
- **CLI 命令行工具**：完整支持无 GUI 批量补帧，支持 `--hardware` 查看本机硬件自适配画像。

---

## 💻 系统与硬件要求
- **芯片**：Apple Silicon 全系（M1 / M2 / M3 / M4 系列及其 Pro / Max / Ultra 款）；
- **系统**：macOS 15 Sequoia 或更高版本；
- **内存**：建议 8GB 及以上（4K 素材建议 16GB 及以上）。

---

## ⚠️ 首次运行说明
由于个人开源项目未加入苹果付费开发者计划，首次启动时 Gatekeeper 会拦截：
- **方式一（推荐）**：在访达里 **右键点击 Macrife.app → 选择「打开」**，随后在弹窗中点击「打开」；
- **方式二（终端）**：执行 `xattr -cr /Applications/Macrife.app`。
